#include "rcsim/core/tree_reduce.hpp"

// §2.4: tree_reduce is a header-only template. This translation unit exists to
// satisfy §15 CMakeLists enumeration; no implementation is placed here.
// Specializations and CI hooks (parallel_hash == sequential_hash, test 06) live in tests/.

namespace rc::sim::core {
// TODO(phase 1, §2.4): add any non-template helpers here if the template grows.
}  // namespace rc::sim::core
