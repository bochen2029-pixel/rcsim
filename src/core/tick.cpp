#include "rcsim/core/tick.hpp"

// §2.5: Ticks are uint64_t; no wall-clock time. Header-only; no implementation needed.

namespace rc::sim::core {
// TODO(phase 0, §2.5): currently header-only. Revisit if tick-duration helpers appear.
}  // namespace rc::sim::core
