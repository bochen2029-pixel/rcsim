#include "rcsim/state/global_state.hpp"

// §3.3: GlobalState pure-data; ParamBlock fields deferred to phase 1.

namespace rc::sim::state {
// TODO(phase 1, §3.3): populate with ParamBlock helpers once parameter list is pinned.
}  // namespace rc::sim::state
