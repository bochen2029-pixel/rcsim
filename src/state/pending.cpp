#include "rcsim/state/pending.hpp"

// §5.2: PendingEffect variant is pure-data; no method implementations needed.

namespace rc::sim::state {
// TODO(phase 2, §5.2): add variant-construction helpers if needed.
}  // namespace rc::sim::state
