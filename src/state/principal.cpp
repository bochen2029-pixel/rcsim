#include "rcsim/state/principal.hpp"

// §3.4: PrincipalState is pure-data; no method implementations in v1.3 scaffold.

namespace rc::sim::state {
// TODO(phase 2, §3.4): populate with any PrincipalState lifecycle helpers as they appear.
}  // namespace rc::sim::state
