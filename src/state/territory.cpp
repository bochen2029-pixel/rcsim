#include "rcsim/state/territory.hpp"

// §3.1-§3.2: TerritoryState is a pure-data struct; no method implementations needed.
// This translation unit exists to satisfy §15 CMakeLists enumeration.

namespace rc::sim::state {
// TODO(phase 1, §3.1): add any non-inline TerritoryState helpers here if they grow.
}  // namespace rc::sim::state
