#include "rcsim/state/canonicalization_state.hpp"

// §2.7: CanonicalizationState holds the Kahan residual sidecar (non-hashed).
// Pure data; actual residual-update logic lives in core/canonicalize.cpp.

namespace rc::sim::state {
// TODO(phase 1, §2.7): populate residual-vector initializer helpers if convenient.
}  // namespace rc::sim::state
