#include "rcsim/political/clock.hpp"

// §7.2: PoliticalClock is pure-data; regime-typed logic in response_latency.cpp.

namespace rc::sim::political {
// TODO(phase 3, §7.2): clock tick/update helpers as implementation proceeds.
}  // namespace rc::sim::political
