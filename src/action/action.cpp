#include "rcsim/action/action.hpp"

// §5.1: Action variant is pure-data; action-specific apply logic lives in apply_with_fizzle.cpp.

namespace rc::sim::action {
// TODO(phase 2, §5.1): add variant-construction helpers if needed.
}  // namespace rc::sim::action
