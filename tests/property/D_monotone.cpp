#include <catch2/catch_test_macros.hpp>

// §16.2, §4.1 RC-7, §4.2: Property — D_local and D_global monotone non-decreasing.
// TODO(phase 1, §4.1, §16.2): dot(D_local) = G * κ ≥ 0; tree-reduce preserves monotonicity.
TEST_CASE("placeholder for D_monotone", "[property][pending]") {
    REQUIRE(false);
}
