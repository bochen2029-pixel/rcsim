#include <catch2/catch_test_macros.hpp>

// §16.2, §3.3, §4.2: Property — A (capability ratio) monotone non-decreasing (Diamond Floor).
// TODO(phase 1, §4.2, §16.2): random-scenario sweep; GlobalState.A never decreases tick-to-tick.
TEST_CASE("placeholder for A_monotone", "[property][pending]") {
    REQUIRE(false);
}
