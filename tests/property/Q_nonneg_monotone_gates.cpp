#include <catch2/catch_test_macros.hpp>

// §16.2: Property — Q_elec, Q_liq stay non-negative; gates monotone in their argument.
// TODO(phase 1, §4.1, §4.5, §16.2): random-scenario sweep; Q fields never go below 0;
//   Theta_soft and Pi_hard monotone non-decreasing.
TEST_CASE("placeholder for Q_nonneg_monotone_gates", "[property][pending]") {
    REQUIRE(false);
}
