#include <catch2/catch_test_macros.hpp>

// §16.1: Test 04 — action-order canonicalization.
// TODO(phase 2, §5.3, §16.1): submit the same set of actions in multiple input orders;
//   assert identical StateHash after Phase 2a canonical-order sort.
TEST_CASE("placeholder for 04_action_order_invariance", "[determinism][pending]") {
    REQUIRE(false);
}
