#include <catch2/catch_test_macros.hpp>

// §16.1: Determinism suite test 02 — checkpoint replay.
// TODO(phase 4, §10.2, §16.1): write sync log, replay from preceding checkpoint,
//   assert per-tick hash equality with original run.
TEST_CASE("placeholder for 02_checkpoint_replay", "[determinism][pending]") {
    REQUIRE(false);
}
